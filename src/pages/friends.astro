---
import friendsJson from "../content/friends.json";
import I18nKey from "../i18n/i18nKey";
import { i18n } from "../i18n/translation";
import MainGridLayout from "../layouts/MainGridLayout.astro";

const friends = friendsJson as {
	title: string;
	image: string;
	desc: string;
	url: string;
	tags?: string[];
}[];
---
<MainGridLayout title={i18n(I18nKey.friends)} description={i18n(I18nKey.friends)}>
  <div class="flex w-full rounded-[var(--radius-large)] overflow-hidden relative min-h-32">
    <div class="card-base z-10 px-9 py-6 relative w-full ">
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-x-6 gap-y-8 my-4">
        {friends.map((item) => (
          <a href={item.url} target="_blank">
            <div class="group relative p-4 -m-4 rounded-[var(--radius-large)]">
              <div class="pointer-events-none absolute inset-0 bg-[var(--enter-btn-bg-hover)] opacity-0 transition-opacity duration-200 group-hover:opacity-30 rounded-[var(--radius-large)]"></div>
              <div class="relative z-10 flex flex-nowrap items-stretch h-28 gap-4 rounded-[var(--radius-large)]">
                <div class="w-28 h-28 flex-shrink-0 rounded-lg overflow-hidden bg-zinc-200 dark:bg-zinc-900">
                  <img src={item.image} alt="站点头像" class="w-full h-full object-cover">
                </div>
                <div class="grow w-full">
                  <div class="font-bold transition text-lg text-neutral-900 dark:text-neutral-100 mb-1">{item.title}</div>
                  <div class="text-50 text-sm font-medium">{item.desc}</div>
                  <div class:list={["items-center", {"flex": true, "hidden md:flex" : false}]}>
                    <div class="flex flex-row flex-nowrap items-center">
                      {(item.tags && item.tags.length > 0) && item.tags.map((tag,i) => (
                        <div class:list={[{"hidden": i==0}, "mx-1.5 text-[var(--meta-divider)] text-sm" ]}>
                          /
                        </div>
                        <span class="transition text-50 text-sm font-medium">{tag}</span>))
                      }
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </a>
        ))}
      </div>
    </div>
  </div>
</MainGridLayout>
